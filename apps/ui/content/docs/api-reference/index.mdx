---
title: Overview
description: Every endpoint, request/response schema, and a live try-it client — generated straight from the published OpenAPI spec. No API key.
---

Every endpoint in the registry's public API, grouped by domain in the sidebar. Each page
documents the real request/response schema and includes a live client — send a request
against the actual endpoint straight from the page, no API key needed.

This reference is generated directly from the published [OpenAPI spec](/schemas) — if a page
here disagrees with the API's actual behavior, the spec (not this page) is what needs fixing.

<Callout type="info">
  Looking for prose walkthroughs instead of a per-endpoint reference? See the [GraphQL](/docs/graphql),
  [RPC](/docs/rpc), and [chain events](/docs/chain-events) guides.
</Callout>
